"""q_block — root package for the qBlock electronic structure library.

Sub-packages
============
- ``q_block.compute`` — core quantum-chemistry framework (MUSE architecture).
- ``q_block.learn``   — documentation, examples, and requirements.
- ``q_block.tests``   — verification and validation test suites.
"""
